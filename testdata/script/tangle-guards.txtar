# The refusal a tangle owes its reader: a block with no language,
# which org would skip in silence, and a target that is the book.

! exec babble tangle no-lang.org
stderr 'without a language'


-- no-lang.org --
#+begin_src
nothing names this
#+end_src
